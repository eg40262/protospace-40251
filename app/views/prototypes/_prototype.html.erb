<div class="card">
  <% if prototype.image.attached? %>
    <%= link_to image_tag(prototype.image, class: :card__img), prototype_path(prototype.id) %>
    <%# image_tag(prototype.image, class: :card__img )： image_tag
        はRailsのViewヘルパーメソッドで、<img>タグを作成します。
        ここでは、画像のソースとしてprototype.image（特定のPrototypeのイメージ）を選択し、
        クラス名としてcard__imgを指定します。 %>
    <%# prototype_path(prototype)： prototype_pathはRailsのルーティングヘルパーメソッドで、
        prototype（特定のPrototype）の詳細ページへのパスを生成します。%>
  <% end %>
  <div class="card__body">
    <%= link_to prototype.title, prototype_path(prototype.id), class: :card__title %>
    <p class="card__summary">
      <%= prototype.catch_copy %>
    </p>
    <%= link_to "by #{prototype.user.name}", user_path(prototype.user), class: :card__user %>
  </div>
</div>